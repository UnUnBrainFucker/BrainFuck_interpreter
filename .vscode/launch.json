{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Brainfuck",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/brainfuck_interpreter.py",
            "args": ["${file}"], // Add this line
            "preLaunchTask": "tasks.json"
        }
    ]
}